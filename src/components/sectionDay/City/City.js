import React, {Component} from 'react';
import './City.scss';

const City = (props) => {
    return ( 
        <div className='container-city'>
            <span>MINSK</span>
        </div>
    );
};

export default City;